# Context Priming  

## MINIMIZE LLM LIMITATIONS

   Use the native `think` tool if you are able to write notes, use tools between thoughts 
   Or start `sequential_thinking` Model Context Protocol server; think while you review the following 
   Maintain project state via updates for across AI instance flow 

### PROJECT-STATE UPDATES (CHECKPOINTS)

   Start the `memory` MCP tool 
    - If starting a project, hold this to last and create the Entity
    - If returning to a project, then query the entity as an EXACT search term 
    - Ask Sean for the name of the Entity to search if it isn't at the top of the session task document 
   Create/Use entity `mao-<project-task-name>` 
   Add entry milestones that maintain context even if suddenly disconnected  
    1. About to start a series of tasks, record what you're about to do 
    2. Also record the next steps in case connection is interrupted during first tasks 
    3. Add updates throughout completing the tasks, particularly anything notable or necessary for next steps
    4. Update after completion of those tasks; add what is next or reference having mentioned it if nothing has changed 

### READ IN FULL 

   `./CLAUDE.md` --> Complete development rules including new UI Development Guidelines section
   `./documentation/10_AI_DEV_INDEX.md` --> Complete Python backend architecture understanding
   `./AUDIT_LOGIC/MAO_FLOW/00_MAO_FLOW_CHAPTERS.md`--> Normal language app functionality described 